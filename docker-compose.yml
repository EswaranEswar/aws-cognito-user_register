version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - /app/node_modules
    environment:
      - NODE_ENV=development
    env_file:
      - .env
    command: ["pnpm", "run", "start:dev"]

volumes:
  mongodb_data: